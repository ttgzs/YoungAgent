# V3.7 Project Connector

Provider-neutral connector for Microsoft Project data. Supports Project XML and CSV immediately; binary `.mpp` is intentionally handled by an adapter boundary so an approved commercial/Project Server provider can be plugged in without changing Agent tools.
