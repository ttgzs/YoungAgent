# V3.9 IoT / SCADA / TDengine Connector

Provider adapters are optional. MQTT is used for live telemetry ingestion; TDengine is used for time-series querying when configured. The connector never exposes direct PLC writes.
