# V3.7 Project Connector
Project XML/CSV are first-class inputs. Binary MPP is routed to a provider adapter instead of unsafe text parsing. Adapter boundary supports Project Server or a licensed SDK.
