# V3.5 BIM MCP

BIM MCP provides IFC text summary, element extraction and quantity aggregation. The interface is designed so IfcOpenShell, Revit API or Autodesk connectors can be plugged in without changing Agent Runtime.
